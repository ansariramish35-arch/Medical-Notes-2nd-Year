import Link from "next/link";
import { notFound } from "next/navigation";
import { systems, papers, topics, questions, figures } from "@/lib/static-data";
import { renderMarkdown, type FigurePayload } from "@/lib/markdown";
import { EmphasisBadge, TypeBadge, Stars } from "@/components/Badges";
import { ProgressToggle } from "@/components/ProgressToggle";

export function generateStaticParams(){ return topics.map((t)=>({id:String(t.id)})); }
export const dynamicParams=false;

export default function TopicPage({params}:{params:{id:string}}){
  const topicId=Number(params.id);
  if(!Number.isFinite(topicId)) notFound();
  const topic=topics.find((t)=>t.id===topicId); if(!topic) notFound();
  const system=systems.find((s)=>s.id===topic.systemId); if(!system) notFound();
  const paper=papers.find((p)=>p.id===system.paperId);
  const mappedQuestions=questions.filter((q)=>q.topicId===topicId).sort((a,b)=>a.sortOrder-b.sortOrder);
  const siblings=topics.filter((t)=>t.systemId===system.id).sort((a,b)=>a.sortOrder-b.sortOrder);
  const figuresBySlug=new Map<string,FigurePayload>(); figures.forEach((f,i)=>figuresBySlug.set(f.slug,{slug:f.slug,title:f.title,caption:f.caption,svg:f.svg,number:i+1}));
  const {html,toc}=renderMarkdown(topic.body,figuresBySlug);
  const idx=siblings.findIndex((t)=>t.id===topicId); const prev=idx>0?siblings[idx-1]:null; const next=idx>=0&&idx<siblings.length-1?siblings[idx+1]:null;
  return <div className="rise"><nav className="mb-5 flex flex-wrap items-center gap-2 text-[12px] font-bold text-ink-faint"><Link href="/" className="hover:text-teal-deep">Console</Link><span aria-hidden>/</span><span>{paper?.name}</span><span aria-hidden>/</span><Link href={"/systems/"+system.id} className="hover:text-teal-deep">{system.unitLabel} · {system.name}</Link></nav><header className="mb-6 border-b-2 border-ink pb-5"><div className="flex flex-wrap items-center gap-2"><EmphasisBadge emphasis={topic.emphasis}/><span className="badge badge-teal">MASTER TOPIC</span><span className="badge badge-ink">{mappedQuestions.length} SOURCE QUESTION{mappedQuestions.length===1?"":"S"} MAPPED</span></div><h1 className="mt-3 font-display text-[30px] font-semibold leading-[1.15] lg:text-[36px]">{topic.title}</h1><div className="mt-4"><ProgressToggle entityType="topic" entityId={topic.id} initialDone={false}/></div></header><div className="grid gap-8 xl:grid-cols-[minmax(0,1fr)_230px]"><article><div className="md-content" dangerouslySetInnerHTML={{__html:html}}/><section className="mt-10"><h2 className="font-display text-[24px] font-semibold" style={{borderBottom:"2px solid var(--color-ink)",paddingBottom:8}}>Layer 4 — One-Minute Revision</h2><div className="callout callout-highyield mt-4"><div className="callout-head"><span className="callout-badge">Rapid recall</span></div><div className="callout-body">{topic.oneMinute.split("\n").filter(Boolean).map((line,i)=><p key={i} className="my-1.5 leading-relaxed"><strong className="text-amber">▪</strong> {line.replace(/^-\s*/,"")}</p>)}</div></div><div className="callout callout-exam mt-3"><div className="callout-head"><span className="callout-badge">Must-use exam keywords</span></div><div className="callout-body"><p className="my-2">{topic.examKeywords}</p></div></div></section><section className="mt-10"><h2 className="font-display text-[24px] font-semibold" style={{borderBottom:"2px solid var(--color-ink)",paddingBottom:8}}>Source Questions Covered</h2><div className="table-wrap mt-3"><table className="w-full text-[13px]"><thead><tr className="bg-ink text-left text-[10.5px] font-extrabold uppercase tracking-[0.09em] text-paper"><th className="px-3 py-2">Source question</th><th className="px-3 py-2">Type</th><th className="px-3 py-2">Priority</th><th className="px-3 py-2">Status</th></tr></thead><tbody>{mappedQuestions.map(q=><tr key={q.id} className="border-t border-line-soft bg-white"><td className="px-3 py-2.5 leading-relaxed">{q.text}</td><td className="px-3 py-2.5"><TypeBadge qtype={q.qtype}/></td><td className="px-3 py-2.5"><Stars n={q.stars}/></td><td className="px-3 py-2.5"><span className="badge badge-teal">COMPLETE</span></td></tr>)}</tbody></table></div></section><nav className="mt-10 flex flex-col gap-3 border-t border-line pt-6 sm:flex-row sm:justify-between">{prev?<Link href={"/topics/"+prev.id} className="panel max-w-sm p-3.5 hover:border-teal">← Previous topic · {prev.title}</Link>:<span/>}{next&&<Link href={"/topics/"+next.id} className="panel max-w-sm p-3.5 text-right hover:border-teal">Next topic → {next.title}</Link>}</nav></article><aside className="hidden xl:block"><div className="sticky top-8 space-y-4"><div className="panel p-4"><p className="text-[10.5px] font-extrabold uppercase tracking-[0.14em] text-ink-faint">In this chapter</p><div className="toc-list mt-2">{toc.map((t)=><a key={t.id} href={"#"+t.id}>{t.text}</a>)}</div></div></div></aside></div></div>;
}